version: '3.8'

services:
  kibana:
    image: kibana:8.6.1
    container_name: {{ ansible_hostname }}-kibana
    environment:
      - SERVERNAME=kibana
      - ELASTICSEARH_HOSTS=http://{{elasticsearch}}:9200
    ports:
      - 5601:5601
    restart: unless-stopped